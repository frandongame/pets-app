version: '3.9'
services:
  app:
    build: .
    container_name: pets-api
    environment:
      SPRING_PROFILES_ACTIVE: local
    depends_on:
      db:
      condition: service_healthy
    ports:
      - "8080:8080"
 